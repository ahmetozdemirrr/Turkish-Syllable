name: Build and Publish Wheels

on:
  push:
    branches: [main]
  release:
    types: [published]

jobs:
  build-wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
      fail-fast: false  # Bir platform başarısız olsa bile diğerleri devam etsin

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install build wheel
        if [ "${{ matrix.os }}" == "ubuntu-latest" ]; then pip install auditwheel; fi

    - name: Install build tools (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        choco install -y visualstudio2019buildtools
        choco install -y visualstudio2019-workload-vctools
      shell: cmd

    - name: Build wheel (Linux and macOS)
      if: matrix.os != 'windows-latest'
      run: |
        python -m build --config-setting=--build-option=--fPIC
      continue-on-error: true

    - name: Build wheel (Windows)
      if: matrix.os == 'windows-latest'
      run: |
        python -m build
      continue-on-error: true

    - name: Repair wheel for Linux (manylinux)
      if: matrix.os == 'ubuntu-latest'
      run: |
        auditwheel repair dist/turkish_syllable-*-cp310-cp310-linux_x86_64.whl
      continue-on-error: true

    - name: Upload wheels
      uses: actions/upload-artifact@v4
      with:
        name: wheels-${{ matrix.os }}
        path: |
          dist/*.whl
          wheelhouse/*.whl
      continue-on-error: true

  publish:
    needs: build-wheels
    runs-on: ubuntu-latest
    if: github.event_name == 'release' && github.event.action == 'published'

    steps:
    - name: Download wheels
      uses: actions/download-artifact@v4
      with:
        path: dist
        pattern: wheels-*
        merge-multiple: true

    - name: Publish to PyPI
      env:
        TWINE_USERNAME: __token__
        TWINE_PASSWORD: ${{ secrets.PYPI_TOKEN }}
      run: |
        pip install twine
        twine upload dist/*